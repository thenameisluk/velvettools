Usage vtflash <version> </dev/diskname>
will flash a bootable image to the first, or to a specified partition
and make it always bootable

unless there is a, untested partition, made using vttest.

<optional>
