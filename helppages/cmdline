A kernel specific cmdline is placed at /boot/cmdline-VERSION

An additional custom cmdline is placed at /boot/cmdline-custom

The root parameters are automatically detected by the tools.

Both the cmdlines and the root parameters

are combined and passed to the kernel at boot.
