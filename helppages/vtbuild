Usage vtbuild <version> <option>
is used to build bootable depthcharge image,
specifying a kernel version is optional.

It will also generate a cmdline at /boot/cmdline
if there are none.

available options :
noinitgen - will not generate new initramfs

<optional>
