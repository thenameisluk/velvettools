Usage vttest <version> </dev/diskname>
will flash a bootable image to the second, or to a specified partition
and make it boot from it once.

If everything works and you want it to be permanent, use vtflash.

<optional>
